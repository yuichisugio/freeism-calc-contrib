#!/bin/bash

# 各タスク・期間ごとの貢献度の重み付け

set -euo pipefail

# 実行期間ごとの重み付け
# 「ライブラリ作成日」と「最後のコミット日」を期間として、5分割して、最初期・初期・中期・後期・最終期ごとに重みづけ


# コード行数の重み付け


# 反応速度の重み付け


# タスクの種類ごとに重みづけ


# 「重み付け」の掛け算



# ユーザーごとの重み付け後の値すべてを足し算する
